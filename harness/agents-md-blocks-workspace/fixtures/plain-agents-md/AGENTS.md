# AGENTS.md

Shared rules for `acme-api`. This is the pre-install state: an ordinary AGENTS.md with no
managed blocks yet, the input a repo presents the first time it runs more than one of the
skills that splice into this file.

## House rules

- Prefer the repo's own scripts over ad-hoc shell.
- Never delete with `rm`; use `trash`.

## Build

```bash
make build
```
