# LOCAL FIX: hand-edited, must not be silently overwritten
#
# This stands in for a hand patch someone applied directly to an installed copy of the
# engine before setup-secret-guard existed, or before this machine ever adopted a later
# revision of the shipped payload. It is deliberately NOT byte-identical to the payload
# this skill ships, so the installer must refuse to replace it without --adopt.
