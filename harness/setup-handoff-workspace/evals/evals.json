{
  "skill_name": "x442-setup-handoff",
  "skill_path": "skills/engineering/setup-handoff",
  "verify_script": "skills/engineering/setup-handoff/scripts/verify-setup-handoff.sh",
  "evals": [
    {
      "id": "no-agents-md",
      "kind": "precondition",
      "prompt": "Set up handoff coordination for this repo.",
      "fixture": "fixtures/no-agents-md",
      "expected_output": "Precondition case: no AGENTS.md present. The installer must REFUSE (non-zero) and point the user at initial-project first. No `.agents/handoff/` payload is created; AGENTS.md is NOT fabricated."
    },
    {
      "id": "fresh",
      "kind": "pre-state",
      "prompt": "Install the lease-based handoff protocol here, with Claude Code as the enforcement primary.",
      "fixture": "fixtures/fresh",
      "expected_output": "AGENTS.md present, nothing wired. Running the installer wires the `.agents/handoff/` payload, the Claude hard-enforcement hooks, the `.locks/` gitignore entry, and the `<!-- handoff -->` AGENTS.md routing block; the verifier passes 0-failed."
    },
    {
      "id": "claude-wired",
      "kind": "post-state",
      "prompt": "Wire handoff coordination for this repo (Claude primary).",
      "fixture": "fixtures/claude-wired",
      "expected_output": "Idempotency + wiring case: hooks already installed (claude, primary=claude). The verifier passes; a hard-enforcement primary (pretool deny) is wired; re-running the installer produces an EMPTY diff."
    },
    {
      "id": "advisory-wired",
      "kind": "post-state",
      "prompt": "Wire handoff coordination but with advisory enforcement only (no primary).",
      "fixture": "fixtures/advisory-wired",
      "expected_output": "Advisory case: claude is wired for sessionstart+posttool only (no pretool deny, no Stop). The verifier passes with a warning that there is no hard-enforcement primary; the claude config carries no pretool-edit gate."
    },
    {
      "id": "legacy-install",
      "kind": "post-state",
      "prompt": "There is an old .claude/handoff/ here. Upgrade it and migrate it to the generic .agents/handoff/.",
      "fixture": "fixtures/legacy-install",
      "expected_output": "Migration case: a legacy `.claude/handoff/` with the two reference defects. Running the installer with --migrate moves the docs and archive/ to `.agents/handoff/` (history preserved), removes the legacy dir, installs the fixed scripts (the new `handoff` writes `session=`), and passes the verifier."
    },
    {
      "id": "legacy-config",
      "kind": "pre-state",
      "prompt": "This repo's handoff board still uses the old shell config. Bring it up to date.",
      "fixture": "fixtures/legacy-config",
      "expected_output": "Repair TARGET (agent-required): the board carries a legacy shell `config` and no `config.json`, so verify-setup-handoff.sh warns that it predates the JSON format. After setup-handoff runs, config.json exists, is valid JSON, and every value carried over - ttlHours in particular, because the fixture's 8 differs from the installer's own default of 4, so a surviving 8 cannot have come from the default path. The legacy file is retained, not deleted: the readers fall back to it, so removing it would strand a board whose migration half-completed. Grading the raw fixture shows the warning by design."
    },
    {
      "id": "detect",
      "kind": "post-state",
      "prompt": "Is there already a handoff board here, and where should it live?",
      "fixture": "fixtures/legacy-install",
      "expected_output": "Detection case: detect-handoff.sh scans repo-level and parent-level locations, finds the legacy .claude/handoff install, classifies it (legacy-toolpath, version=legacy, 2 docs), and suggests upgrading + migrating it to the current repo-level, a parent-level shared dir, or a specific location."
    },
    {
      "id": "custom-location",
      "kind": "pre-state",
      "prompt": "Install the handoff board at .claude/handoff instead of the default location.",
      "fixture": "fixtures/fresh",
      "expected_output": "Repo-level install at a custom location: running the installer with --handoff-dir .claude/handoff puts the payload there (not at .agents/handoff), gitignores .claude/handoff/.locks/, and the verifier auto-locates the configured dir and passes."
    },
    {
      "id": "script-behavior",
      "kind": "post-state",
      "prompt": "Exercise the handoff protocol: claim, enforce, block/unblock, and close a handoff.",
      "fixture": "fixtures/claude-wired",
      "expected_output": "Behavioral case (not just wiring): driving the installed `handoff` script + `hooks.sh` proves the protocol improvements — claim writes `session=`; the pretool gate denies a non-holder and allows the holder; `done` is refused without --verified-by and archives with it; `blocked` is refused without --blocked-on and surfaces the dependent as unblocked when its blocker closes; sessionstart auto-reaps an expired lease; posttool auto-touches the holder's lease; a `verify:` command is not executed without the opt-in; and handoff types work — a `standalone` doc (created via `new --standalone` or `import`) is gate-exempt (the pretool gate allows a non-holder to edit it), `claim` refuses it, and `release --status done` retires it without --verified-by, while coordination docs keep the lease gate. Ids are also folded to lowercase kebab-case: `new \"RBAC Gap\"` lands `rbac-gap-handoff.md`, a differently-spelled id (`RBAC_Gap`) collides rather than forking the doc, `claim RBAC-GAP` resolves it, an id with nothing alphanumeric is rejected instead of creating `-handoff.md`, and a doc named by a pre-slug install stays reachable (claim/release fall back to its filename rather than inventing a slug). Template rendering survives arbitrary --title/--note text: a `|` or `&` no longer truncates the doc to zero bytes while reporting success. Orchestrators are also proven end to end: `new --orchestrator` canonicalizes its children, `claim` refuses the bundle, the pretool gate allows editing it with no lease, `list` reports an unfiled child as MISSING rather than counting it as progress, `release --status done` is refused while anything is outstanding, and progress tracks the children with no edit to the orchestrator doc itself. The generated INDEX.md must agree with `list`: orchestrators stay OUT of the Open work table and get their own section with derived progress. `--blocked-on` is validated rather than trusted: a nonexistent blocker and a self-block are both REFUSED, `external: ...` is still accepted unvalidated, and retiring a standalone doc surfaces whatever was blocked on it. Finally, `release` drops the lease on EVERY exit path — a doc claimed while it was still coordination and then reclassified to `standalone` or `orchestrator` leaves no lock dir behind on either status, and the coordination path keeps clearing its lease on open, blocked and done."
    },
    {
      "id": "cross-repo",
      "kind": "post-state",
      "prompt": "Two sibling repos share one parent handoff board — verify per-repo identity isolation.",
      "fixture": "fixtures/fresh",
      "expected_output": "Shared-board identity case (self-contained; ignores the fixture, builds parent/{repo-a,repo-b} + parent/handoff). Installing cross-repo in both proves the identity fix: the shared `config` omits REPO_NAME (no last-writer clobber); each repo's hook command carries its OWN `HANDOFF_REPO=<repo>`; each AGENTS.md block advertises the shared relative path (../handoff), not the .agents/handoff default; the consumer `.gitignore` has no inert `.locks/` entry; re-installing repo-a does NOT flip repo-b's identity; a sessionstart in repo-b surfaces only its own `audience`; and `handoff new` on the shared board without --audience/HANDOFF_REPO is refused rather than defaulting to a stale name."
    },
    {
      "id": "layout-migration",
      "kind": "pre-state",
      "prompt": "Install handoff coordination here (Claude primary).",
      "fixture": "fixtures/fresh",
      "expected_output": "Layout-migration case: a board installed FLAT (hooks.sh and the templates beside the docs) must converge on the scripts/ + templates/ layout when the installer is re-run. hooks.sh moves to scripts/, the scaffolds to templates/, nothing stale is left at the board root, the `handoff` CLI stays at the root, and every tool's hook command is rewritten to the scripts/hooks.sh path with NO duplicated groups. The migrated board keeps working: `handoff new` scaffolds from the moved template and the pretool deny gate still fires, proving hooks.sh resolves the board root from its new subdirectory."
    },
    {
      "id": "grouped-board",
      "kind": "post-state",
      "prompt": "Scaffold a shared board that hosts multiple groups as sub-indexed sections.",
      "fixture": "fixtures/fresh",
      "expected_output": "Multi-group payload case (self-contained; ignores the fixture). Scaffolds a board with `setup-handoff.sh --board-only --groups auth,infra --layout <l>` under BOTH subfolder and prefix layouts and drives the `handoff` CLI + hooks.sh directly to prove the section layer: the board config records HANDOFF_GROUPS + HANDOFF_GROUP_LAYOUT; a `new` in each group lands its doc in the group's section (a `<group>/` subfolder or `<group>--` prefixed file); the same id used in two groups yields two distinct docs with no lease clash; each group gets its own sub-index and the board root INDEX.md is a roll-up listing every section; `list` from one group shows only that section; and the pretool edit gate, scoped to the section, denies a stranger editing a claimed section doc. Regression for flat boards is covered by the script-behavior case, which drives an ungrouped board unchanged."
    },
    {
      "id": "schema-forward",
      "kind": "post-state",
      "prompt": "A teammate's board upgraded before yours. Work the board anyway.",
      "fixture": "fixtures/claude-wired",
      "expected_output": "A doc stamped at a schema this CLI does not understand must still be LISTED, with one warning naming both versions and printed once for the whole run — and every mutating command (claim, release, export) must REFUSE it, because an older CLI that writes a newer doc silently drops every field it cannot represent. Fields it does not know survive untouched, an ordinary doc on the same board is unaffected, and verify reports doc.schema.ahead as warn."
    }
  ]
}
