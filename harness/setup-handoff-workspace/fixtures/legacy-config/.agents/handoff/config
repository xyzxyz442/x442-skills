# Generated by setup-handoff. Read by handoff + hooks.sh.
TOPOLOGY=single-repo
REPO_NAME=claude-wired
# 8, deliberately NOT the installer's default of 4: a surviving 8 after migration cannot have
# come from the default path, so this value is what makes the legacy-config eval provable.
HANDOFF_TTL_HOURS=8
