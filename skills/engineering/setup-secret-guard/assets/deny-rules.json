{
  "_comment": [
    "Permission rules the secret guard owns. These cover the Read and Edit tools, whose output",
    "a PreToolUse hook cannot filter — the hook can rewrite a shell read into a redacted read,",
    "but it cannot redact what a file-reading tool returns, so those paths are denied outright.",
    "",
    "ONE canonical form per path. An earlier hand-maintained list carried both '**/x' and",
    "'//**/x' twins for about fifteen patterns; the installer emits this form only and reports",
    "unmanaged duplicates rather than deleting them, because a deny rule removed on a guess is a",
    "silent hole.",
    "",
    "read: redaction would be meaningful but the tool cannot be intercepted, so deny.",
    "edit: raw key material and credential stores, where an edit is a rewrite of a secret."
  ],
  "read": [
    "**/.env",
    "**/.env.*",
    "**/.npmrc",
    "**/.pypirc",
    "**/.netrc",
    "**/*.pem",
    "**/*.key",
    "**/*.p12",
    "**/*.pfx",
    "**/id_rsa*",
    "**/id_ed25519*",
    "**/id_ecdsa*",
    "**/id_dsa*",
    "~/.ssh/**",
    "**/kubeconfig",
    "**/kubeconfig*",
    "**/*.kubeconfig",
    "~/.kube/**",
    "~/.aws/credentials",
    "~/.aws/config",
    "~/.config/gcloud/**",
    "~/.docker/config.json",
    "~/.claude/.credentials.json",
    "**/credentials.json",
    "**/secrets.json",
    "**/secrets.yaml",
    "**/secrets.yml",
    "/etc/hosts",
    "**/hosts"
  ],
  "edit": [
    "**/*.pem",
    "**/*.key",
    "**/*.p12",
    "**/*.pfx",
    "**/id_rsa*",
    "**/id_ed25519*",
    "~/.ssh/**",
    "**/kubeconfig",
    "**/kubeconfig*",
    "**/*.kubeconfig",
    "~/.kube/**",
    "~/.aws/credentials",
    "~/.claude/.credentials.json"
  ]
}
