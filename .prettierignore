node_modules
dist
coverage
package-lock.json
pnpm-lock.yaml
yarn.lock
.venv
.code-review-graphignore

# Byte-critical harness fixture: intentionally invalid JSON that the repair-graph-hooks eval
# must see verbatim. Keep the pre-commit prettier hook from choking on / rewriting it.
harness/repair-graph-hooks-workspace/fixtures/broken-json/.claude/settings.local.json

# Generated eval-benchmark artifacts (aggregate.py output). Keep the generator's own formatting so
# re-running `aggregate.py` reproduces the committed file byte-for-byte instead of churning on
# prettier vs json.dumps array style.
harness/**/iterations/**/benchmark.json
harness/**/iterations/**/benchmark.md

# Byte-critical handoff harness fixtures: committed verbatim so the setup-handoff idempotency
# eval (which re-runs the installer) sees the installer's own output, and so the {{}}-free doc
# template placeholders survive. Prettier must not rewrite them.
harness/setup-handoff-workspace/fixtures/**
harness/run-handoff-workspace/fixtures/**

# Generated handoff board index (`handoff` CLI output). Its markdown tables are unaligned, which
# prettier rewrites; every subsequent handoff command regenerates them unaligned again, so the file
# would churn on every claim/release. Keep the generator's own formatting, same as benchmark.* above.
.agents/handoff/INDEX.md
